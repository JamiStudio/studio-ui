---
kind: fixed
area: contracts
---

Hardened the Phase 2 shared-seam coverage gate so matrix-only `runEvent` samples
must use exact harness event types and matrix-only `actionRef` samples must prove
their named lifecycle, replay, missing-source, secret-bearing, and display-only states.
