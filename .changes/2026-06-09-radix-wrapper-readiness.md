---
type: feature
surface: ui
---

Added a source-locked Radix wrapper readiness contract for the resident UI
vocabulary. The contract records current official Radix and shadcn registry
sources, enumerates the evidence required before any wrapper can be claimed, and
keeps every current wrapper, runtime React renderer, hosted runtime, backend
persistence, and backend registration claim explicitly unsupported.
