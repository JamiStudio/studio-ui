# UI Vocabulary Foundation

Added the first authored `packages/ui` resident primitive/component vocabulary with
tokenized styles, accessibility/state metadata, source-provenance metadata, renderer
allowlist entries, registry-addressable items, and tests that reject hardcoded component
color literals. Generated registry artifacts now embed real install content for the
initial vocabulary source/style files instead of leaving the button primitive source-pending.
