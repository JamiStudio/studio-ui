Public release and hosted registry evidence

- Record the public `0.1.0` Studio UI package release, npm provenance metadata,
  GitHub release assets, and `registry.jami.studio` custom-domain hosted registry
  smoke as supported evidence.
- Extend hosted live smoke to verify public cache/content headers and remote CLI
  installs for theme, primitive, page, block, and suite items from the custom
  domain.
- Add `--registry-url` as a CLI alias for the documented `--registry` option
  without changing the documented install path.
- Keep hosted persistence, backend package registration, hosted suite runtime,
  harness execution, and final brand canon explicitly unclaimed.
