## Fix

- Reconciled workbench, release, registry-publishing, and roadmap status surfaces
  after the resident vocabulary registry items became installable generated content.
- Updated the workbench test to assert generated installability from live registry
  metadata instead of the older `button` source-pending state.
