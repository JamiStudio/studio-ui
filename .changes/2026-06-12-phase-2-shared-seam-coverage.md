---
type: changed
area: contracts
---

Add the Phase 2 shared seam coverage matrix for Studio UI and wire it into
`pnpm contracts:check` / `pnpm verify`, including registry metadata drift checks
for the matrix path and coverage id.
